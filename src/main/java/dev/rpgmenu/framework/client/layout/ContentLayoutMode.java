package dev.rpgmenu.framework.client.layout;

/** Declares which outer content shell a semantic top-level tab receives. */
public enum ContentLayoutMode {
    INVENTORY_SPLIT,
    FULL_CONTENT,
    CUSTOM
}
